summary: Get all inventory items
tags:
  - Inventory
responses:
  "200":
    description: List of inventory items
    content:
      application/json:
        schema:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
                example: 1
              name:
                type: string
                example: "Rice"
              category:
                type: string
                example: "Grains"
              unit:
                type: string
                example: "kg"
              minimum_stock:
                type: number
                format: float
                example: 10.0
              current_stock:
                type: number
                format: float
                example: 50.0
              status:
                type: string
                example: "Available"
  "500":
    description: Server error